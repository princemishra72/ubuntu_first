name: Use Secret Example

on: [push]

jobs:
  print-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print secret safely
        run: echo "My secret is ${{ secrets.MY_SECRET }}"

